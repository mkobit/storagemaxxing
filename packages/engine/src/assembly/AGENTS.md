# assembly/

## Responsibility
This package models the planning domain.
It represents the logical spaces, furniture structure, rules, and global limits across the application.


## Import rules
- **May import from**: `geometry/`, `catalog/`.
- **Must not import from**: `engine/`, `solver/`, `store/`, `workers/`, `ui/`.
