{
  "formula": "openspec-sync",
  "description": "Hydrate Beads from OpenSpec tasks.md. Transitions change to 'Implemented' stage.",
  "version": 2,
  "type": "workflow",
  "vars": {
    "change_name": {
      "description": "The OpenSpec change folder name",
      "required": true
    },
    "domain": {
      "description": "Target domain label",
      "required": false
    },
    "scope": {
      "description": "Target scope label",
      "required": false
    }
  },
  "steps": [
    {
      "id": "expand-tasks",
      "title": "Expand Tasks from {{change_name}}",
      "type": "task",
      "labels": [
        "type:sync",
        "meta:openspec"
      ],
      "description": "Run `bd create --file openspec/changes/{{change_name}}/tasks.md` to map all tasks into Beads."
    }
  ]
}
