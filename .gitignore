node_modules
dist
.env
*.log
*.pid
.screenshots/
playwright-report/
test-results/
storybook-static/
.eslintcache

# Beads auto-export backup — Dolt is the source of truth (bd dolt push/pull).
# Never commit this file; it is rewritten on every bd command and pollutes diffs.
.beads/issues.jsonl

# bd 1.2+ Smart Gate runtime lock (repo root, not inside .beads/ — created during
# schema-migration checks, not covered by .beads/.gitignore's *.lock pattern).
.beads.gate.lock

# Personal Claude Code overrides (settings.json is the shared, checked-in config).
.claude/settings.local.json

# Wrangler local dev/deploy cache.
.wrangler/
