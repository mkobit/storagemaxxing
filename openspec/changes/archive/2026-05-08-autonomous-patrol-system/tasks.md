## 1. Infrastructure Setup

- [x] - **Validation**: `ls -d .jules/prompts/` succeeds.
- [x] - **Validation**: `bd formula list` shows the new formula.

## 2. Initial Patrol Protocols

- [x] - **Validation**: File exists and contains valid markdown.
- [x] - **Validation**: File exists and contains valid markdown.

## 3. Verification

- [x] - **Validation**: A new bead is created with the `meta:patrol` label and correct description.
