#!/bin/bash
# Move engine types to assembly, or adjust the import rules.
# PRD says "engine/ may only import from geometry/ and catalog/ (and assembly/ once it exists — see src/engine/AGENTS.md)".
# The AGENTS.md in src/engine says "Must not import from: ... assembly/".
