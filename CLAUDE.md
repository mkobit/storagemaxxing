@AGENTS.md

> **Note:** This file has no local Beads Integration block. The `@AGENTS.md` import above already pulls in AGENTS.md's full `profile:full` block, including the hand-patched branch+PR fix (see AGENTS.md's own warning banner, tracked in sm-sws7). If `bd setup claude --check` reports the integration missing or stale, that's expected — do not run `bd setup claude` to "fix" it; regenerating would add back a redundant, un-patched duplicate here.
