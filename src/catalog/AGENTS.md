# catalog — agent mandate

**Mandate:**
This directory contains static vendor data and pure lookup functions ONLY.

**Hard Rules:**
- NO side effects. NO network requests.
- Only pure functions and constant data objects.
- Permitted dependencies: `model`.
- NO imports from `ui`, `store`, `engine`, `solver`, or `workers`.
