# ui/constraints — agent mandate

**Mandate:**
This directory contains the UI components for editing and displaying constraint definitions.

**Hard Rules:**
- All business logic goes in `engine` or `store`.
- Follow the rules of the parent `ui` package.
